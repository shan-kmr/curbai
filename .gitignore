venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ipynb_checkpoints/
.DS_Store

# OSMnx HTTP response cache (auto-populated on fetch)
cache/

# Re-creatable raw data — too large / irrelevant to check in
data/raw/
data/*.pkl

# data/sf_scored.parquet and data/category_counts_sf.parquet ARE checked in
# (small, needed at runtime) so the app boots on fresh clones

# Secrets
.streamlit/secrets.toml
.env
