venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ipynb_checkpoints/
.DS_Store

# OSMnx HTTP response cache (auto-populated on fetch)
cache/

# Re-creatable raw data — too large / irrelevant to check in
data/raw/
data/*.pkl

# data/sf_scored.parquet IS checked in (98 KB) so the app boots on fresh clones

# Secrets
.streamlit/secrets.toml
.env
